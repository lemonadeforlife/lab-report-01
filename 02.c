/*
Problem Number: 02
Problem Name: Write a C program to input two numbers and find their sum.
*/
#include <stdio.h> // Standard Input Output Header Library Function has been declared

int main() // Main Function has been declared
{ // Code Block has started from here
  int x,y; // two integer variable x & y has been declared
  scanf("%d%d", &x, &y); // scanf is taking two input from user for x & y variable
  printf("%d", x+y); // Then it it's adding those two variable and printing it
  return 0; // Finally returning 0 for indicating successful execution
} // Code Block has ended here
